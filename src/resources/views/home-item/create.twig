{% extends '@AdminBundle/layout.twig' %}
{% block title %}店铺装修{% endblock %}
{% set leftMenuActive='Home' %}

{% block contentRight %}

    <div class="page-title">店铺装修-配置项</div>

    <div class="content-top">
        <ul class="top-nav">
            <li class="active"><a href="">{{ tagString }}管理</a></li>
        </ul>
        <div class="clearfix"></div>
    </div>

    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">

                {# 错误提示信息 #}
                {% if error is defined and error is not empty %}
                    <div class="ui-tips bg-info">
                        <span class="icon glyphicon glyphicon-info-sign"></span>
                        <span>{{ error }}</span>
                    </div>
                {% endif %}

                {# 表单 #}
                <form action="{{ url('admin/home-item/create') }}" method="post" class="form-horizontal mt15">

                    {{ include('@AdminBundle/home-item/_form.twig') }}

                    <div class="form-group">
                        <div class="col-sm-offset-2 col-sm-8">
                            <button type="submit" class="btn btn-primary">提交</button>
                        </div>
                    </div>

                </form>

            </div>
        </div>
    </div>
{% endblock %}

{% block js %}
    {% include '@AdminBundle/home-item/_js.twig' %}
{% endblock %}
